# GitHub Pages 배포 방법

`site` 폴더 안의 세 파일을 그대로 올리면 됩니다. 터미널이나 Git 지식은 필요 없습니다.

```
site/
  index.html    ← 포트폴리오 사이트
  guide.html    ← 클라이언트 서비스 소개서
  .nojekyll     ← 빈 파일 (지우지 마세요)
```

---

## 1. 저장소 만들기

1. [github.com](https://github.com) 로그인 → 우측 상단 **+** → **New repository**
2. **Repository name** 에 `portfolio` 입력 (원하는 이름으로 해도 됩니다)
3. **Public** 선택 — Pages 무료 사용 조건입니다
4. **Create repository**

## 2. 파일 올리기

1. 만들어진 저장소 화면에서 **uploading an existing file** 링크 클릭
2. `index.html` 과 `guide.html` 을 드래그해서 올립니다
3. **Commit changes** 클릭

### `.nojekyll` 은 따로 만드세요

숨김 파일이라 드래그가 안 될 수 있습니다. 그럴 땐:

1. 저장소 화면에서 **Add file** → **Create new file**
2. 파일명에 `.nojekyll` 입력 (내용은 비워둡니다)
3. **Commit changes**

> 이 파일이 없으면 밑줄(`_`)로 시작하는 파일이나 일부 경로가 무시될 수 있습니다. 지금 구조에선 없어도 대개 동작하지만, 넣어두는 게 안전합니다.

## 3. Pages 켜기

1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. **Source** 를 **Deploy from a branch** 로
3. **Branch** 를 `main`, 폴더는 `/ (root)` 로 지정 → **Save**
4. 1~2분 뒤 새로고침하면 주소가 나옵니다

```
https://<깃허브아이디>.github.io/portfolio/
```

소개서는 뒤에 `guide.html` 을 붙이면 됩니다.

```
https://<깃허브아이디>.github.io/portfolio/guide.html
```

---

## 배포하면 자동으로 해결되는 것

**유튜브 153 오류가 사라집니다.** 파일을 직접 열 때(`file://`)는 origin 이 없어 유튜브가 임베드를 거부하지만, GitHub Pages 는 https 로 서빙되므로 정상 재생됩니다. 히어로 배경 영상, 카드 호버 프리뷰, 상세 페이지 재생이 전부 살아납니다.

로컬 안내 바(주황색 띠)도 자동으로 사라집니다. 그 바는 `file://` 일 때만 뜨도록 되어 있습니다.

---

## 수정할 때

파일을 고친 뒤 GitHub 저장소에서 해당 파일을 열고 연필 아이콘(**Edit**)을 눌러 내용을 바꾸거나, 같은 이름으로 다시 업로드하면 덮어써집니다. 1분 안에 사이트에 반영됩니다.

자주 고치실 곳은 `index.html` 안 `<script>` 상단의 `WORKS` 배열입니다. 작업물 제목·수치·스토리가 전부 거기 모여 있습니다.

---

## 올리기 전 채워야 할 것

- [ ] `[이름]` — 헤더 로고, 푸터 2곳
- [ ] 연락처 — X, Instagram URL (`href="#"` 로 비어 있습니다)
- [ ] 폼 수신 주소 — 현재는 전송이 연결돼 있지 않습니다
- [ ] `guide.html` 의 이메일·전화·포트폴리오 주소
- [ ] 각 작업물의 `story` 본문 (비어 있으면 점선 안내가 보입니다)

> 사이트 주소가 정해지면 `guide.html` 의 포트폴리오 주소 칸에 넣으세요. 소개서와 사이트가 서로 연결됩니다.

---

## 나중에 개인 도메인을 붙이려면

도메인을 사시면 Settings → Pages → **Custom domain** 에 입력하고, 도메인 업체 DNS 설정에서 GitHub Pages 주소를 가리키게 하면 됩니다. 필요하실 때 말씀해 주세요.
