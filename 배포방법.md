# 배포 방법

이 폴더를 저장소 루트에 그대로 올리면 됩니다 (기존 `index.html` 덮어쓰기).

```
index.html
support.js
image-slot.js
_ds/classical-3fe54fed-98b7-4c73-8b6d-2567542cb374/styles.css
_ds/classical-3fe54fed-98b7-4c73-8b6d-2567542cb374/_ds_bundle.js
```

기존 `guide.html`(서비스 소개서)은 그대로 두세요 — 의뢰 섹션에서 링크하고 있습니다.

## 확인

- 로컬에서 볼 때는 폴더에서 `python3 -m http.server 8000` 실행 후 `localhost:8000` 으로 접속하세요. 파일을 직접 열면(file://) 유튜브 임베드가 차단됩니다.
- GitHub Pages(Settings → Pages → main 브랜치)로 배포하면 그대로 동작합니다.

## 내용 수정

`index.html` 안 `WORKS` 배열이 작업물 목록입니다. 항목마다:

- `yt` — 유튜브 영상 ID (빈 값이면 "영상 링크 준비 중" 판으로 표시)
- `seek` — 호버 미리보기 시작 초
- `title` / `client` / `year` / `dur` / `desc` / `role` / `views`

## 아직 비어 있는 것

- 보정 전 · 후 비교 스틸 두 장, 작업자 사진 (사이트에서 드래그&드롭으로 채울 수 있음)
- Instagram · X 계정 링크
- 문의 폼 수신 주소 (현재는 화면 안내만 표시)
